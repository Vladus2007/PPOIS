namespace Types
{
    public enum ArtworkCondition { Excellent, Good, Fair, Poor, Damaged }
    public enum HallType { Permanent, Temporary, Special, Outdoor }
    public enum SecurityLevel { Low, Medium, High, Maximum }
    public enum TransactionType { Income, Expense, Transfer }
    public enum AccountType { Operating, Reserve, Donation }
    public enum InvoiceStatus { Draft, Sent, Paid, Overdue }
    public enum CameraType { Fixed, PTZ, Thermal, Infrared }
    public enum AlarmType { Burglar, Fire, Environmental }
    public enum AccessLevel { Public, Staff, Curator, Admin }
    public enum VisitorType { Regular, Student, Senior, Group, VIP }
    public enum TicketType { Regular, Discount, Group, VIP }
    public enum MembershipType { Basic, Premium, VIP, Corporate }

    public enum ArtistStyle { PrimaryStyle}
}